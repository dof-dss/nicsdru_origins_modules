# Schema for the configuration files of the Origins: Forms module.
origins_forms.settings:
  type: config_object
  label: 'Origins: Forms settings'
  mapping:
    form_descriptions:
      type: boolean
      label: 'Enable'
