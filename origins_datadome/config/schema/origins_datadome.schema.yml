# Schema for the configuration files of the Origins DataDome module.
origins_datadome.settings:
  type: config_object
  label: 'Origins datadome settings'
  mapping:
    ddjskey:
      type: string
      label: 'DataDome JS key'
